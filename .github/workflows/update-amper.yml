name: Amper Auto-Update

on:
  workflow_call:
    inputs:
      force_update:
        description: 'Force update even if version is unchanged'
        required: false
        default: false
        type: boolean

permissions:
  contents: write

jobs:
  update-amper:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Get current Amper version
        id: before
        run: |
          VERSION=$(./amper --version 2>&1 | grep -oP 'version \K[\d.]+')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Current Amper version: $VERSION"

      - name: Run amper update
        run: ./amper update || true

      - name: Get new Amper version
        id: after
        run: |
          VERSION=$(./amper --version 2>&1 | grep -oP 'version \K[\d.]+')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "New Amper version: $VERSION"

      - name: Check for changes
        id: changes
        run: |
          if git diff --quiet && [ "${{ inputs.force_update }}" != "true" ]; then
            echo "has_changes=false" >> $GITHUB_OUTPUT
          else
            echo "has_changes=true" >> $GITHUB_OUTPUT
          fi

      - name: Run amper test
        if: steps.changes.outputs.has_changes == 'true'
        run: ./amper test

      - name: Commit and push changes
        if: steps.changes.outputs.has_changes == 'true'
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "chore: update Amper ${{ steps.before.outputs.version }} → ${{ steps.after.outputs.version }}"
          git push

      - name: Summary
        run: |
          if [ "${{ steps.changes.outputs.has_changes }}" == "true" ]; then
            echo "### ✅ Amper updated and tests passed" >> $GITHUB_STEP_SUMMARY
            echo "**${{ steps.before.outputs.version }}** → **${{ steps.after.outputs.version }}**" >> $GITHUB_STEP_SUMMARY
          else
            echo "### ℹ️ Already on latest Amper version (${{ steps.before.outputs.version }})" >> $GITHUB_STEP_SUMMARY
          fi
